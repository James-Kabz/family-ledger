# Authentication
ADMIN_PASSWORD=replace-with-strong-password

# Feature flag (default false uses local in-memory/json repository)
USE_DB=false

# Optional: local dev JSON path override
LEDGER_DEV_FILE=./data/family-ledger.dev.json

# Optional WhatsApp export formatting
TARGET_BUDGET_KES=1700000
WHATSAPP_BUDGET_LINE=Our total budget is *ksh.1.7M* (inclusive of hospital bill and burial preparations budget)

# Prisma / Neon (only needed when USE_DB=true)
DATABASE_URL=postgresql://USER:PASSWORD@HOST/db?sslmode=require
DIRECT_URL=postgresql://USER:PASSWORD@HOST/db?sslmode=require
